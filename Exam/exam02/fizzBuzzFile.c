/**
 * TODO: author header, documentation
 */
#include <stdlib.h>
#include <stdio.h>

#include "utils.h"

int main(int argc, char **argv) {

  if(argc !=2) {
    fprintf(stderr, "Usage: %s fileName\n", argv[0]);
    exit(1);
  }


  return 0;
}
